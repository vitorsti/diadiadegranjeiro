using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using EventProperties;

public class DailyTask : MonoBehaviour
{
    //[SerializeField] RegionManager regionManager;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    RegionProperties[] get = GameObject.FindObjectsOfType<RegionProperties>();
    //    foreach (RegionProperties rp in get)
    //    {
    //        if (rp.region == this.gameObject.GetComponent<EventHeader>().props.regions[0])
    //            regionManager = rp.gameObject.GetComponent<RegionManager>();
    //    }
    //}

    //public void ActivateTask()
    //{
    //    Debug.Log("ActivateTask: " + gameObject.name);
    //}

    //private void Update()
    //{

    //    if (Input.GetKeyDown(KeyCode.M))
    //    {
    //        if (regionManager != null)
    //            regionManager.dailyTaskComplete = true;

    //        Destroy(gameObject);
    //    }
    //}
}
